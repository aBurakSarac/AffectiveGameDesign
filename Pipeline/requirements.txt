# La Façade Fissuréе — Python pipeline dependencies
# Python 3.12.12 | conda env: facade | OS: Windows 11
# Install: conda activate facade && pip install -r requirements.txt

# ── Core FER pipeline ─────────────────────────────────────────────────────────
mediapipe==0.10.33
hsemotion-onnx==0.3.1
onnxruntime==1.25.0

# ── Computer vision ───────────────────────────────────────────────────────────
opencv-python==4.13.0.92

# ── Numerical and data ────────────────────────────────────────────────────────
numpy==2.4.4
pandas==3.0.2
scipy==1.17.1

# ── Deep learning (required by hsemotion-onnx) ────────────────────────────────
torch==2.11.0
torchvision==0.26.0
timm==1.0.26

# ── Visualisation ─────────────────────────────────────────────────────────────
matplotlib==3.10.9

# ── File I/O ──────────────────────────────────────────────────────────────────
openpyxl==3.1.5         # annotation .xlsx support

# ── System utilities ──────────────────────────────────────────────────────────
psutil==7.2.2           # memory / CPU monitoring in benchmark HUD
rich==15.0.0            # terminal progress output

# ── rPPG signal processing ────────────────────────────────────────────────────
scikit-learn>=1.3.0     # ICA algorithm (FastICA)
PyWavelets>=1.5.0       # WAVELET algorithm (Morlet CWT)

# ── GPU (optional — CPU-only by default, set USE_GPU=True in fer/test_mp_hs.py) ──
# onnxruntime-gpu==1.25.0
